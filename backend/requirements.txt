# Digikart — backend dependencies (Python 3.14, Django 6.0)
# Pinned to the versions installed and verified for the project.

# Core
Django==6.0.1
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-environ==0.13.0
django-filter==25.2

# Google Sign-In: verifies the Google ID token (signature + audience + issuer)
# server-side in apps/accounts/google.py — Google is the only signup/login
# method. Uses the `requests` transport pinned below.
google-auth==2.55.1

# Database & images
psycopg[binary]==3.3.4
Pillow==12.2.0

# Payments
razorpay==2.0.1

# YouTube Data API calls for the admin playlist import (apps/content/youtube.py).
# Already present transitively via razorpay/boto3; pinned since we import it directly.
requests==2.34.2

# PDF processing at admin upload time (apps/content/note_files.py): validates
# the upload and generates the compressed fast-preview rendition. Pure pip
# wheel (bundles MuPDF) — no system Ghostscript/qpdf needed, so it works on
# PaaS hosts like Render. Notes are watermarked CLIENT-SIDE (pdf.js draws the
# buyer's email + name on each page canvas), not here.
pymupdf==1.27.2.3

# PDF generation for the test suite only — apps/content/tests.py synthesizes
# sample PDFs with it.
reportlab==4.5.1

# Production: WSGI server, static serving, Cloudflare R2 media storage
# (S3-compatible, via django-storages + boto3), Redis cache
gunicorn==26.0.0
whitenoise==6.12.0
django-storages[s3]==1.14.6
boto3==1.43.18
django-redis==6.0.0
